theme = catppuccin-frappe
font-family = "JetBrains Mono"
auto-update = check
quick-terminal-position = center
unfocused-split-opacity = 0.8

# https://ghostty.org/docs/help/terminfo
shell-integration-features = ssh-terminfo

# mac, and ubunutu files are gitignored. Add file based on OS to use fish.
# ubuntu - command = /usr/bin/fish --login --interactive
# mac    - command = /opt/homebrew/bin/fish --login --interactive
config-file=?mac
config-file=?ubuntu

keybind = global:cmd+grave_accent=toggle_quick_terminal
keybind = ctrl+b>r=reload_config

# Commented to not conflict with tmux
#
# keybind = ctrl+a>c=new_tab
# keybind = ctrl+a>n=next_tab
# keybind = ctrl+a>p=previous_tab
# keybind = ctrl+a>1=goto_tab:1
# keybind = ctrl+a>2=goto_tab:2
# keybind = ctrl+a>3=goto_tab:3
# keybind = ctrl+a>4=goto_tab:4
# 
# keybind = ctrl+a>-=new_split:down
# keybind = ctrl+a>;=new_split:right
# keybind = ctrl+a>h=goto_split:left
# keybind = ctrl+a>j=goto_split:bottom
# keybind = ctrl+a>k=goto_split:top
# keybind = ctrl+a>l=goto_split:right
# keybind = ctrl+a>ctrl+h=resize_split:left,100
# keybind = ctrl+a>ctrl+j=resize_split:down,100
# keybind = ctrl+a>ctrl+k=resize_split:up,100
# keybind = ctrl+a>ctrl+l=resize_split:right,100
# keybind = ctrl+a>x=close_surface
# keybind = ctrl+a>enter=toggle_split_zoom

keybind = shift+enter=text:\x1b\r

